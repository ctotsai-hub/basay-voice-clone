--- a/inference.py	2026-06-22 03:21:19.736001442 +0000
+++ b/inference.py	2026-06-22 03:21:19.739001442 +0000
@@ -18,6 +18,7 @@
 import time
 
 import torchaudio
+import soundfile as sf
 import librosa
 from modules.commons import str2bool
 
@@ -141,7 +142,8 @@
         def semantic_fn(waves_16k):
             ori_inputs = whisper_feature_extractor([waves_16k.squeeze(0).cpu().numpy()],
                                                    return_tensors="pt",
-                                                   return_attention_mask=True)
+                                                   return_attention_mask=True,
+                                                   sampling_rate=16000)
             ori_input_features = whisper_model._mask_input_features(
                 ori_inputs.input_features, attention_mask=ori_inputs.attention_mask).to(device)
             with torch.no_grad():
@@ -404,7 +406,14 @@
     source_name = os.path.basename(source).split(".")[0]
     target_name = os.path.basename(target_name).split(".")[0]
     os.makedirs(args.output, exist_ok=True)
-    torchaudio.save(os.path.join(args.output, f"vc_{source_name}_{target_name}_{length_adjust}_{diffusion_steps}_{inference_cfg_rate}.wav"), vc_wave.cpu(), sr)
+    # NOTE: newer torchaudio (2.9+) dropped its built-in WAV encoder in favor of the
+    # separate `torchcodec` package, which isn't installed here. Use soundfile instead
+    # (already a hard dependency via requirements-mac.txt) to avoid that extra dependency.
+    sf.write(
+        os.path.join(args.output, f"vc_{source_name}_{target_name}_{length_adjust}_{diffusion_steps}_{inference_cfg_rate}.wav"),
+        vc_wave.cpu().numpy()[0],
+        sr,
+    )
 
 
 if __name__ == "__main__":
